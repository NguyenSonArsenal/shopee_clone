@AGENTS.md

[//]: # (- Hãy tham khảo source nextjs ở cty: D:\project\ecomobi-v2\donation\passio-frontend để gợi ý cách làm )

- `AdminPagination` (component/admin) dùng chung cho mọi màn list: chỉ truyền `page/totalPages/onPageChange` → phân trang 2 item
- truyền thêm `perPage/perPageOptions/onPerPageChange` → tự bật layout 3 item (thêm select số dòng/trang).
- Khi viết css, nếu giá trị đã được lưu trong biến trong file variable.scss, hãy dùng nó
 
